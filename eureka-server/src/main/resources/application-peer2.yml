server:
  port: 1112

spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: localhost
  client:
#register-with-eureka: false
    ##    fetch-registry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:1111/eureka/
